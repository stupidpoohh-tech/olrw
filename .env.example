# Neon 콘솔 → Connect 에서 호스트와 데이터베이스 이름을 가져온다.
# 접속 문자열이 아니라 https 주소다. 자격증명도 쿼리스트링도 붙이지 않는다.
#   예) https://ep-xxxx-yyyy.c-2.ap-southeast-1.aws.neon.tech/neondb
# SDK 가 여기에서 인증 주소와 Data API 주소를 각각 유도한다.
VITE_NEON_URL=

# 비워 두면 앱은 memoryStore(브라우저 저장소)로 돈다. 혼자 둘러보기에는 충분하고,
# 둘이서 주고받으려면 위 값을 채워야 한다.
