# Supabase 프로젝트 설정 → API 에서 가져온다.
# Vercel Project Settings 에도 Production / Preview 양쪽에 같은 값을 넣는다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# service_role 키는 절대 VITE_ 접두어로 두지 않는다. 클라이언트 번들에 그대로 들어간다.
